spring:
  profiles:
    active: dev
  application:
    name: spring-cloud-config
server:
  port: 8777
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS